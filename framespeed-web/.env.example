# Supabase 프로젝트 URL 과 anon 키.
#
# anon 키는 브라우저에 실리는 게 정상입니다 — 방어선은 키가 아니라 DB 의 RLS 와 GRANT 입니다.
# 그래도 실제 값은 저장소에 올리지 않습니다. 이 파일을 .env 로 복사해서 채우세요.
#
# service_role 키는 어떤 경우에도 여기 두지 않습니다.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>
