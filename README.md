# test-claude

Hello World 웹페이지입니다.

## 로컬에서 보기

`index.html` 파일을 브라우저로 열면 됩니다.

## GitHub Pages로 배포하기

1. 저장소 **Settings** → **Pages** 이동
2. **Source**를 `Deploy from a branch`로 선택
3. Branch를 `main`(또는 배포할 브랜치), 폴더를 `/ (root)`로 지정 후 **Save**
4. 잠시 후 `https://<사용자명>.github.io/test-claude/` 에서 확인
